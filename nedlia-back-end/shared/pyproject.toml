[project]
name = "nedlia-shared"
version = "0.1.0"
description = "Shared domain models and utilities"
requires-python = ">=3.11"
dependencies = [
    # Validation
    # "pydantic>=2.5.0",
]

[tool.uv]
dev-dependencies = []
